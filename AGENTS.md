# AGENTS.md

本文件为`ChangAn_ARS_optim`的项目级AGENTS.md

## 0. ***文件权限与修改边界***（重要，需严格遵守）

### 0.1 `LX_project` 权限

`LX_project` 路径：`E:\WPS Office\1628575652\WPS企业云盘\清华大学\我的企业文档\课题组相关\理想项目\LX_project`

对于 `LX_project`，只有只读权限，严禁修改或删除。

可以读取 `LX_project` 中的代码、文档和类似模块实现细节，用于理解项目 A 的设计和作为项目 B 的参考，但不得修改 `LX_project` 下任何文件。

### 0.2 `ChangAn_ARS_optim` 权限

`ChangAn_ARS_optim` 路径：`D:\CodeProject\ChangAn_ARS_optim`

`ChangAn_ARS_optim` 是项目 B 的主工作目录。

其中，以下内容只有只读权限，严禁修改或删除：

* `ChangAn_ARS_optim/reference`；
* 原始数据文件：`ChangAn_ARS_optim/data/raw/injury_data.xlsx`。

`ChangAn_ARS_optim` 中，除上述只读内容外，其它文件夹和文件有读写权限，可以根据项目 B 的开发需要进行修改。

### 0.3 原始数据保护

不得直接修改原始数据文件 `ChangAn_ARS_optim/data/raw/injury_data.xlsx`。

如需清洗、转换、筛选或生成中间数据，应输出到新的派生数据文件或中间结果目录中，并在代码或说明中明确其来源和生成方式。

## 1. 项目背景与目录关系

甲方 L 就是 LX（理想），甲方 C 就是 changan（长安）。

`LX_project` 是为与甲方 L 的横向项目 A（理想项目）完成的代码项目工作目录。
`ChangAn_ARS_optim` 是为与甲方 C 的横向项目 B（长安项目）完成的代码项目工作目录。

当前需要为甲方 C 的横向项目 B 提供一套类似的“自适应约束系统参数优化算法”，但不能和给甲方 L 的技术方案相似度过高。甲方 C 的要求比较低，并且课题组内对项目 B 的重视程度低于项目 A。
因此，为了应付项目 B 的交付，在一些技术细节上我允许适当妥协/作弊（比如允许在测试集上进行超参调优），但禁止在README、技术文档/报告、汇报PPT等交付物或者其它甲方 C 可能接触的材料中提及这些，避免暴露和尴尬。

项目 B 的整体逻辑仍然是：给定上游工况输入，完成约束系统参数优化。

## 2. 项目 A 与项目 B 的差异定位

项目 A 是为甲方 L 完成的代码项目，工作目录为 `LX_project`。
项目 A 明确包含三个子项目/交付物：

* 碰撞波形预测模型；
* 乘员损伤预测模型；
* 自适应约束系统参数优化算法。

项目 B 是为甲方 C 完成的代码项目，工作目录为 `ChangAn_ARS_optim`。
项目 B 当前只要求一个主被动一体化控制策略，即自适应约束系统参数优化算法，作为交付物。

因此，项目 B 不需要像项目 A 一样分成 `PulsePredict`、`InjuryPredict`、`ARS_optim` 三个子项目。项目 B 只需要一个主项目工作目录，损伤预测部分可以直接做简化版本，不需要单独拆成一个子项目。

## 3. 项目 B 的总体设计原则

项目 B 可以参照项目 A 的代码项目和技术思路，但应做减法和降级，形成适合项目 B 的“自适应约束系统参数优化算法”。

设计时需要注意：

1. 不引入复杂的碰撞波形/烈度预测模块，或者至少不能单独拆出一块复杂的子项目/工作。项目 B 不应像项目 A 一样形成复杂的三子项目结构。

2. 项目 B 不要求“碰撞波形预测”，也对具体乘员损伤风险预测模型本身没有很高要求。碰撞波形数据存在，但使用优先级不高。

3. 项目 B 能用的数据无论质量还是数量都差于项目 A。数据格式类似仿真数据，但仿真模型更粗糙，因此数据质量较低；数据量大概也是几千条。

4. 考虑数据质量和数量问题，项目 B 建议兼顾精度/性能和鲁棒性。尤其在高速工况下，应重点保证损伤能够降低一定幅度，并考虑预测精度误差、不确定性或置信度等因素。不过，一切技术/修改/设计的终极目的：让我给甲方汇报或交付时的指标结果好看（比如模型精度、算法降损量等关乎项目交付指标的结果），能够满足交付需求，能够唬得住甲方，让甲方觉得你做的很nice。对于鲁棒性指标，可以用一些trick来包装和美化，比如人工调整一些计算方式或系数，让它看上去令人满意、不容易引起争议。

5. 项目 B 在实现上可以相比项目 A 更简化，但最好在“讲故事”、“工程/学术包装层面”有一些小的亮点或创新点，不至于完全做成平平无奇的技术流水账。但我不建议：太繁琐、过度工程化的代理模型修改，也就是那种让人一看，人工痕迹很重、说不清道不明的设计。因为这样的修改/设计，很难对外解释其原理、设计或修改的理由，不好跟甲方交代。

6. 为了区分项目 B 和项目 A，不建议项目 B 使用摊销优化算法。

7. 项目 B 的算法降损效果，可重点关注碰撞速度 `v >= 40 km/h` 的碰撞工况，目标是观察能否实现将乘员损伤严重性（AIS）至少降低 1 个等级。

## 4. 建议阅读和参考的资料

在进行项目 B 的代码开发、算法设计或技术方案整理前，建议优先阅读以下资料（均位于 `ChangAn_ARS_optim/reference/reference material` 中）：

1. `理想项目（LX_project）相关技术文档` 里面的文档是项目 A（理想项目） 的中相关技术方案的介绍，其中《理想项目技术开发方案-碰撞风险预测及自适应防护策略V2》和《LX_数据驱动的自适应约束系统参数优化方法介绍_20260206》可以作为语言表述风格的参考，供项目 B 的技术报告、进度汇报PPT等文档创作时的写作风格参考。《理想项目-乘员损伤风险预测模型-技术报告文档-20260501》和《理想项目-道路实时场景自适应防护策略算法-技术报告文档-20260601》的AI生成痕迹太重，不建议作为文风参考。

2. `ChangAn_ARS_optim` 的《changan_乘员损伤风险仿真数据-可用数据说明.docx》。
   该文档用于了解项目 B（长安项目）的数据、参数含义和参数取值情况。

3. `ChangAn_ARS_optim` 的 changan_Injury_Criteria_AIS_Cal_original.py。该脚本内实现了若干从连续损伤指标到相应损伤等级（AIS）的计算映射关系（来源于经验公式和阈值概率）。原始数据表格（`ChangAn_ARS_optim/data/raw/injury_data.xlsx`）中的损伤等级（AIS）列就是根据该脚本计算得到的。

4. 如遇到不确定的实现细节，应优先回顾已有参考资料、用户给出的信息内容或指令；其次参考 `LX_project` 中类似模块的实现细节。

所有相关文档都是中文。读取 `.docx`、`.xlsx`、`.py` 或其它中文文件时，应注意编码和解析方式，避免读取乱码后做出错误判断。

## 5. 运行环境

默认虚拟环境为 `pytorch`。

在需要运行 Python 脚本、测试代码或实验命令前，优先执行：

```bash
conda activate pytorch
```

如环境缺少依赖、数据文件不存在或命令无法运行，需要在回答中明确说明原因，不要隐瞒未验证项。

## 6. 工况参数与数据含义补充

### 6.1 碰撞角度

碰撞角度表示墙体绕车身前端旋转的角度，逆时针为正方向。

### 6.2 碰撞偏置

碰撞偏置表示墙体向车身前端正面投影线段长度占车身前端正面长度的比例。

重叠部分包含主驾侧则为正，否则为负。

具体取值含义如下：

* `1`：100%；
* `2`：75%；
* `4`：50%；
* `6`：25%；
* `3`：-75%；
* `5`：-50%；
* `7`：-25%。

## 7. 项目 B 的可调参数范围

对于项目 B，可以成为约束系统可调参数，即决策变量/控制变量的只有以下变量：

* 气囊状态：`airbag`；
* 膝部气囊状态：`kneeairbag`；
* 安全带限力：`ll_level`；
* 座椅位置：`delta_pos`；
* 前后仰角：`recline_angle`。

其中，座椅位置和前后仰角可调，是因为将其看作座椅本身的参数；乘员前后位置、背部靠角与座椅位置、靠背角随动。

除上述变量外，不要自行扩展新的约束系统可调参数。若确实需要引入新的决策变量，必须先说明依据、必要性和与已有数据字段的关系。

## 8. 代理模型与损伤指标要求

对于代理模型和后续参数寻优算法，4 个腿部力损伤指标不用考虑。

代理模型建议直接预测基础连续损伤响应：

* `Amax`；
* `Dmax`；
* `HIC`；
* `Nij`。

不要把 `CTI` 作为独立预测目标。已复核 `injury_data.xlsx` 中的 `output_CTI` 可由 `output_Amax`、`output_Dmax` 和 `input_type_num` 按 `4_Injury_Criteria_AIS_Cal.py` 中的公式精确复算。

因此，后处理阶段应按照以下逻辑处理：

1. 由代理模型预测 `Amax`、`Dmax`、`HIC`、`Nij`；
2. 由预测得到的 `Amax`、`Dmax` 计算派生 `CTI`；
3. 分别用经验公式完成损伤等级映射：

   * `HIC -> hic_AIS`；
   * `CTI -> cti_AIS`；
   * `Nij -> nij_AIS`；
4. 取 `hic_AIS`、`cti_AIS`、`nij_AIS` 三者最大值，得到派生 `MAIS`。

## 9. 评估与验证要求

评估时应分层报告结果，不要只给一个总指标。

至少需要包括：

1. 基础连续指标误差：

   * `Amax`；
   * `Dmax`；
   * `HIC`；
   * `Nij`。

2. 派生 `CTI` 误差。

3. 分项 AIS 和 `MAIS` 的等级误差：

   * `hic_AIS`；
   * `cti_AIS`；
   * `nij_AIS`；
   * `MAIS`。

连续损伤指标的误差可以使用 `RMSE`、`MAE`、`R2` 等回归指标衡量。

损伤等级误差可以使用 `accuracy` 等分类指标衡量，不要用类似 `MAE` 的连续指标评判损伤等级误差。

评估时需要重点关注 `v >= 40 km/h` 的高速碰撞工况，并重点检查这些工况下乘员损伤严重性（AIS）是否至少降低 1 个等级。

## 10. 代码开发策略

开发项目 B 时，应优先遵循以下策略：

1. 先理解项目 A 的相关设计，再判断哪些内容适合在项目 B 中降级复用。
2. 优先做面向项目 B 的简化实现，不要照搬项目 A 的三子项目结构。
3. 保持项目 B 的主线清晰：上游工况输入 → 代理模型预测损伤响应 → 派生损伤等级 → 约束系统参数寻优 → 降损效果评估。
4. 对于数据质量较低、数据量有限的问题，应优先考虑稳健、可解释、易验证的工程实现。
5. 不确定的地方一定要回顾已有参考资料和用户给出的信息内容或指令，其次参考 `LX_project` 中类似模块的实现细节。
6. 修改代码时优先采用最小必要改动，避免无关重构和无关文件修改。
7. 如果需要新增脚本、配置文件或实验输出，应保证命名清晰，便于后续区分项目 B 与项目 A。
8. 我已经在使用 git 进行版本管理，因此不需要在实际代码中、函数命名或文件命名上进行版本号标记，比如 `v1`、`v1.5`、`v2` 等。

## 11. 不确定性处理要求

如果遇到以下情况，不要自行猜测或隐式处理：

* 文档内容读取异常或出现乱码；
* 字段含义、取值范围或单位不明确；
* 项目 A 与项目 B 的设计要求可能冲突；
* 用户给出的规则与已有代码实现不一致；
* 需要修改只读目录或只读文件；
* 需要引入新的决策变量、预测目标或评价指标；
* 需要大幅改变项目 B 的总体技术路线。

遇到上述情况时，应先基于已有参考资料、用户给出的信息和 `LX_project` 类似模块进行核对；仍无法确定时，需要在回答中明确指出不确定点、可能影响和建议处理方式。


# 12. 项目修改与性能改进的历史记录

模型/算法性能指标改进尝试（包括成功或失败的经验）、（非性能改进为目的的）历史重要修改（包括模型输入输出等接口变化、特征变化等）等内容，记录在本地 experiments/project_history中，目的是方便后续Agent进行参考，避免非必要的重蹈覆辙、内容倒退、重复无效工作。具体可参见 experiments/project_history/README.md。

# 13. 文档创作、写作输出与对话回答的语言风格要求

务必遵守全局AGENTS.md（本机为`C:\Users\Admin\.codex\AGENTS.md`）中的相关要求，如## 3. 表达风格与去 AI 味、## 4. 文档文字内容创作。
